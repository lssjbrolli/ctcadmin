$("#paper-modal").html("<%= escape_javascript(render 'modal') %>");
$(document).ready(function() {
    $('.datepicker').datepicker({
        format: "yyyy-mm-dd",
        todayBtn: "linked",
        orientation: "top left",
        autoclose: true,
        todayHighlight: true
    });
});