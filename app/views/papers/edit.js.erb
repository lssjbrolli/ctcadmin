$("#modal").html("<%= escape_javascript(render 'modal/modal') %>");
$(document).ready(function() {
	$('#myModalLabel').text("Editing paper...");
    $('.datepicker').datepicker({
        format: "yyyy-mm-dd",
        todayBtn: "linked",
        orientation: "top left",
        autoclose: true,
        todayHighlight: true
    });
});