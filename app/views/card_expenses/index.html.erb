<div class="container">

  <% if flash %>
    <% flash.each do |k,v| %>
      <%= alert(k,v) %>
    <% end %>
  <% end %>

  <h1>Listing Card Expenses</h1>

  <table class="table table-hover table-condensed">
    <thead>
      <tr>
        <th>Internal Number</th>
        <th>Invoice Number</th>
        <th>Date</th>
        <th>Description</th>
        <th>Value</th>
        <th>EUR Value</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @card_expenses.each do |ce| %>
        <tr>
          <td><%= ce.id %></td>
          <td><%= ce.number %></td>
          <td><%= ce.date %></td>
          <td><%= ce.description %></td>
          <td><%= number_to_currency(ce.value, :unit => ce.currency) %></td>
          <td><%= number_to_currency(ce.value_eur, :unit => "EUR") %></td>
          <td class="btn-group btn-group-xs">
            <%= link_to 'Show', ce.file_url, class: "btn btn-info" %>
            <%= link_to 'Edit', edit_card_expense_path(ce), class: "btn btn-info" %>
            <%= link_to 'Destroy', ce, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= will_paginate @p_card_expenses, renderer: BootstrapPagination::Rails %>

  <%= link_to 'New Expense', new_card_expense_path, class: "btn btn-primary" %>
</div>