$("#modal").html("<%= escape_javascript(render 'modal/modal') %>");
$(document).ready(function () {
    var company = "<%= @company.name %>";
    var header = 'Info for: ' + company;
    $('#myModalLabel').text(header);
});