<!DOCTYPE html>
<html lang="en">

<head>
  <title>Cozma Transport 2005 SRL</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <div class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <%= link_to 'CTCAdmin', root_path, class: 'navbar-brand' %>
      </div>

      <div class="collapse navbar-collapse navbar-right" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <% if signed_in? %>
            <li><%= link_to 'Trucks', trucks_path, class: "btn btn-default navbar-btn" %></li>
            <li><%= link_to 'Credit notes', credit_notes_path, class: "btn btn-default navbar-btn" %></li>
            <li><%= link_to 'Credit invoices', credit_invoices_path, class: "btn btn-default navbar-btn" %></li>
            <li class="dropdown">
              <a href="#" role="button" class="dropdown-toggle btn btn-default navbar-btn" data-toggle="dropdown">
                Expenses
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                <li>
                  <%= link_to 'Trip Expenses', trip_expenses_path, role: 'menuitem', tabindex: '-1' %>
                </li>
                <li>
                  <%= link_to 'National Expenses', national_expenses_path, role: 'menuitem', tabindex: '-1' %>
                </li>
                <li>
                  <%= link_to 'International Expenses', extern_expenses_path, role: 'menuitem', tabindex: '-1' %>
                </li>
                <li>
              </ul>
            </li>
            <li><%= link_to 'Companies', companies_path, class: "btn btn-default navbar-btn" %></li>
            <li><%= link_to 'Employees', employees_path, class: "btn btn-default navbar-btn" %></li>
            <% if @current_user.admin? %>
                <li><%= link_to 'Users', users_path, class: "btn btn-default navbar-btn" %></li>
            <% end %>
            <li><%= link_to 'Settings', site_settings_path, class: "btn btn-default navbar-btn" %></li>
            <li class="divider-vertical"></li>
            <li><%= link_to "Logged in as #{fullname(@current_user)}", profile_path, class: "btn btn-default navbar-btn" %></li>
            <li class="divider-vertical"></li>
            <li><%= link_to 'Sign Out', signout_path, method: 'delete', class: "btn btn-default navbar-btn" %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>

<body>
  <% if check_mounted %>
    <%= yield %>
  <% else %>
    <div class="modal fade" id="storage_modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h3 class="modal-title" id="myModalLabel">Storage was not found!</h3>
          </div>
          <div class="modal-body">
            <p>Please mount the storage and reload page.</p>
          </div>
          <div class="modal-footer">
            <%= link_to "Reload", root_path, class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</body>

</html>
