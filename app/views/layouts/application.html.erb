<!DOCTYPE html>
<html>

<head>
  <title>Cozma Transport 2005 SRL</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <div class="navbar navbar-inverse">
    <div class="navbar-inner">
      <%=link_to "CTCAdmin", trucks_path, class: "brand" %>  
      <ul class="nav pull-right">
        <% if signed_in? %>
          <li class="divider-vertical"></li>
          <li><%= link_to "Logged in as #{fullname(@current_user)}", user_path(@current_user) %></li>
          <li class="divider-vertical"></li>
          <li><%= link_to "Sign Out", signout_path, method: "delete" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</head>

<body>
  <div class="tabbable tabs-left">
    <% if signed_in?%>
    <ul class="nav nav-tabs" id="myTab">
      <li class="<%= 'active' if current_page?(trucks_path) %>"><%= link_to "Trucks", trucks_path %></li>
      <li class="<%= 'active' if current_page?(credit_notes_path) %>"><%= link_to "Credit notes", credit_notes_path %></li>
      <li class="<%= 'active' if current_page?(credit_invoices_path) %>"><%= link_to "Credit invoices", credit_invoices_path %></li>
      <li class="<%= 'active' if current_page?(debit_invoices_path) %>"><%= link_to "Debit invoices", debit_invoices_path %></li>
      <li class="<%= 'active' if current_page?(companies_path) %>"><%= link_to "Companies", companies_path %></li>
      <% if @current_user.admin? %>
        <li class="<%= 'active' if current_page?(users_path) %>"><%= link_to "Users", users_path %></li>
      <% end %>
      <form class="form-search">
        <input type="text" class="input-small search-query" placeholder="Search">
      </form>
    </ul>
    <% end %>
    <div class="tab-content">
      <div class="tab-pane active" id="tab">
        <%= yield %>
      </div>
    </div>
  </div>
</body>

</html>
