<% if notice %> 
  <div data-alert class="alert-box.success">
    <%= notice %>
    <a href="#" class="close">&times;</a>
  </div>
<% end %>

<h1>Listing Credit Notes</h1>

<table>
  <thead>
    <tr>
      <th>Number</th>
      <th>Start</th>
      <th>Stop</th>
      <th>Week</th>
      <th>Value</th>
      <th>Paid</th>
    </tr>
  </thead>

  <tbody>
    <% @credit_notes.each do |cn| %>
      <tr>
        <td><%= cn.number %></td>
        <td><%= cn.start %></td>
        <td><%= cn.stop %></td>
        <td><%= cn.week %></td>
        <td><%= cn.value %></td>
        <td><%= cn.paid %></td>
        <td><%= link_to 'Show', cn %></td>
        <td><%= link_to 'Edit', edit_credit_note_path(cn) %></td>
        <td><%= link_to 'Destroy', cn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Credit note', new_credit_note_path, class: "button small round" %>
